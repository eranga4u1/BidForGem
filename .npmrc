# Keep dependency resolution strict and reproducible across web/mobile/api.
auto-install-peers=true
strict-peer-dependencies=false
# Expo / React Native tooling expects a hoisted-enough layout; node-linker=hoisted
# avoids RN metro resolution issues while staying pnpm-based.
node-linker=hoisted
save-exact=false
